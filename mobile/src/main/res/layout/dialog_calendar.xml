<?xml version="1.0" encoding="utf-8"?>
<com.samsistemas.calendarview.widget.CalendarView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="@dimen/padding"
    android:id="@+id/calendarView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:calendarBackgroundColor="@color/white"
    app:calendarTitleTextColor="@color/colorAccent"
    app:currentDayOfMonthColor="@color/white"
    app:dayOfMonthTextColor="@color/accent"
    app:dayOfWeekTextColor="@color/accent"
    app:disabledDayBackgroundColor="@color/white"
    app:disabledDayTextColor="@color/white"
    app:selectedDayBackgroundColor="@color/colorAccent"
    app:titleLayoutBackgroundColor="@color/white"
    app:weekLayoutBackgroundColor="@color/white"/>
